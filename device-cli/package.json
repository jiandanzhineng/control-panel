{
  "name": "device-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Device management CLI scaffold",
  "bin": {
    "device-cli": "./cli.js"
  },
  "scripts": {
    "start": "node cli.js",
    "test:smoke": "node cli.js help",
    "test:mock": "node scripts/selftest.mock.js",
    "test": "npm run test:smoke && npm run test:mock"
  },
  "engines": {
    "node": ">=18"
  }
}
