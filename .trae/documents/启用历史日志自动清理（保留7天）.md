## 结论
* 自动清理未生效：已有 `cleanOldLogs(daysToKeep = 7)`，但未在启动时调用。

## 方案（仅在启动时执行，不做定时）
* 在后端启动入口调用一次清理，确保每次启动都只保留近 7 天。

## 改动点
1. 后端独立运行入口
* `backend/index.js:88-92`（`app.listen(PORT, ...)` 前）调用：`logService.cleanOldLogs()`。

2. Electron 桌面模式入口
* `electron/main.js:133-148`（`server = expressApp.listen(...)` 前）调用：`logService.cleanOldLogs()`。

* 两处都加调用，确保无论以独立后端还是 Electron 方式启动，都会清理。

## 验证
* 重启后检查 `logs` 目录：仅剩近 7 天 `.log` 文件。
* 历史日志页面只显示近 7 天记录。
* 放入一个 10 天前命名的 `.log` 文件，重启后应被删除。

## 备注
* 维持默认保留 7 天；若未来需要可支持 `LOG_RETENTION_DAYS` 环境变量。