services:
  game-platform:
    build: .
    restart: unless-stopped
    init: true
    env_file: .env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - game-platform-data:/var/lib/game-platform

volumes:
  game-platform-data:
