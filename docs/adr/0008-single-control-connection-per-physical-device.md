# 每台物理设备只有一个控制连接

同一个设备 ID 可以同时拥有 MQTT、串口或 BLE 等多个可用设备连接，但设备列表只呈现一个物理设备，并且任一时刻只有一个控制连接负责下发命令。自动发现的新连接不得抢占现有控制连接，控制连接由用户明确切换，并在断开后回退到另一条可用连接；命令不得向多个连接广播，以避免同一硬件动作执行多次。该约束只作用于下行命令：各连接收到的上行业务消息都照常进入设备层，不额外增加跨连接去重；通常用户只采用一种连接，少数同时连接导致的重复触发属于可接受行为。
