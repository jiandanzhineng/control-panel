# 郊狼 3.0 走独立 BLE 协议

摘要：47L 按 Coyote 3.0 控制，不走 2.0 GATT。

决定：
1. 广播名 `47L` 或写特征 `150A` 走 3.0：`180C/150A`，`B0` 每 100ms，连上写 `BF`。
2. 设备类型仍是 `DGLAB`，玩法能力不改。
3. 写特征不得落到 `2a00`；指定 UUID 找不到则失败。
4. 网页蓝牙 V2 选择器不含 `47L`。
